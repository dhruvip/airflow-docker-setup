# airflow-docker-setup
- Clean the project with:
    ``` make clean ```
- Run the project with:
    ``` make init up ```
- Reload scheduler and worker
    ``` make reload  ```
- Build custom image 
    ``` make build ```